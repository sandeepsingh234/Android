package com.example.corefield.fieldjob.Listener;

import android.view.View;

public interface RecyclerTouchListener {
    void onClick(View view, int position);
    void onImageClicked(View view,int position);
}